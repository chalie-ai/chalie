[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -x --strict-markers
markers =
    unit: Unit tests (no external deps)
    integration: Integration tests (need services)
    e2e: End-to-end tests (live network / real chromium)

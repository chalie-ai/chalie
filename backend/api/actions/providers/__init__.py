"""Providers action package — nested verb operations for LLM provider config.

Directory name matches the CRUD endpoint's URL segment (``providers``) 1:1 —
no dash/underscore translation needed, unlike mcp-clients.
"""

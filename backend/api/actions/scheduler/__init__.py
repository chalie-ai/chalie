"""Scheduler action package — nested verb operations for scheduled prompts.

Directory name matches the CRUD endpoint's URL segment (``scheduler``) 1:1 —
no dash/underscore translation needed, unlike mcp-clients.
"""

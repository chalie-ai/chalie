"""MCP clients action package — nested verb operations for outbound MCP servers.

Directory name is underscored (Python packages can't have dashes); the URL
segment stays ``mcp-clients`` because ``api/routes.py`` mounts these under that
slug, matching the CRUD endpoint in ``api/endpoints/mcp_clients.py``.
"""

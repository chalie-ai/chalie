"""Migrated Action subclasses — api/actions/<slug>/<verb>.py, one file, one class.

Every <slug> directory needs its own __init__.py — discovery walks regular
packages only. Subclasses must be constructible with no arguments.
"""

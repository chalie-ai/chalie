# Voice runtime: TTS via Kokoro v1.0 (kokoro-onnx, espeak-ng phonemizer)
# and STT via Moonshine base (useful-moonshine-onnx). Both ship as separate
# ONNX models downloaded by installer/install.sh into resources/voice-models/.
kokoro-onnx>=0.5.0
useful-moonshine-onnx>=20251121
soundfile>=0.13.0
numpy>=1.24.0
